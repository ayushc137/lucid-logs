# Lucid Logs - Production Environment
# Copy to .env in deploy/ directory

APP_ENV=production

# Database
DB_USER=root
DB_PASS=CHANGE_ME_STRONG_PASSWORD
DB_NAMESPACE=daily_journal
DB_DATABASE=core

# Auth - REQUIRED! Generate: openssl rand -base64 32
JWT_SECRET=CHANGE_ME

# Admin account
ADMIN_USERNAME=admin@example.com
ADMIN_PASSWORD=CHANGE_ME
